import 'package:flutter/cupertino.dart';

class SessionStatisticsResume extends StatelessWidget {
  const SessionStatisticsResume({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
